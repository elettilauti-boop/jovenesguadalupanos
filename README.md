# Youth Digital Profile Platform

## Setup

```bash
cp .env.local.example .env.local
```

Fill `.env.local` with your Firebase project values.

## Run

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000)
